<?xml version='1.0' encoding='utf-8'?>
<widget id="com.shiguangju.app" version="1.0.0" xmlns="http://www.w3.org/ns/widgets" xmlns:cdv="http://cordova.apache.org/ns/1.0" xmlns:android="http://schemas.android.com/apk/res/android">
    <name>拾光橘</name>
    <description>拾光橘 - 时间记录工具</description>
    <author email="support@example.com" href="">拾光橘团队</author>
    <content src="index.html" />
    <access origin="*" />
    <allow-intent href="http://*/*" />
    <allow-intent href="https://*/*" />
    <allow-intent href="tel:*" />
    <allow-intent href="sms:*" />
    <allow-intent href="mailto:*" />
    <allow-intent href="geo:*" />

    <!-- 图标配置（请确保根目录下有 icon-192.png 和 icon-512.png） -->
    <icon src="icon-192.png" width="192" height="192" />
    <icon src="icon-512.png" width="512" height="512" />

    <!-- 启动屏新方式 -->
    <preference name="AndroidWindowSplashScreenAnimatedIcon" value="icon-512.png" />

    <!-- 基本偏好 -->
    <preference name="Fullscreen" value="false" />
    <preference name="Orientation" value="portrait" />
    <preference name="DisallowOverscroll" value="true" />
    <preference name="BackgroundColor" value="#FFF7E6" />
    <preference name="ShowTitle" value="false" />

    <!-- Android 特有配置 -->
    <platform name="android">
        <preference name="AndroidExtraFilesystems" value="files,files-external,documents,sdcard,cache,cache-external,root" />
        <preference name="AndroidPersistentFileLocation" value="Compatibility" />
        <preference name="AndroidLaunchMode" value="singleTask" />
        <preference name="AndroidWindowSplashScreenAnimatedIcon" value="icon-512.png" />

        <!-- 必要权限 -->
        <config-file parent="/*" target="AndroidManifest.xml">
            <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
            <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
            <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
            <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
            <uses-permission android:name="android.permission.WAKE_LOCK" />
            <uses-permission android:name="android.permission.VIBRATE" />
            <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
        </config-file>

        <!-- 注册前台服务和广播接收器 -->
        <config-file parent="/manifest/application" target="AndroidManifest.xml">
            <service android:name="com.shiguangju.TimerForegroundService"
                     android:foregroundServiceType="dataSync"
                     android:exported="false" />
            <receiver android:name="com.shiguangju.BootReceiver"
                      android:enabled="true"
                      android:exported="true">
                <intent-filter>
                    <action android:name="android.intent.action.BOOT_COMPLETED" />
                </intent-filter>
            </receiver>
        </config-file>

        <!-- 自定义插件 -->
        <feature name="TimerServicePlugin">
            <param name="android-package" value="com.shiguangju.TimerServicePlugin" />
        </feature>
    </platform>

    <!-- iOS 占位 -->
    <platform name="ios">
        <preference name="deployment-target" value="13.0" />
    </platform>

    <!-- ✅ 已改为小写路径，与新建的 my-float-plugin 文件夹对应 -->
    <plugin name="my-float-plugin" spec="./my-float-plugin" />

    <!-- 引擎最低版本 -->
    <engine name="android" spec="^12.0.0" />
</widget>
